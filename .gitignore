# SQLite database (data lokal, jangan di-commit)
/data/*.sqlite
/data/*.sqlite-journal
/data/*.sqlite-wal
/data/*.sqlite-shm

# Local AI coding assistant config (bukan bagian project)
/.claude/

# Obsidian workspace config (auto-generate saat vault dibuka, beda per device)
/docs/.obsidian/
